services:
  log-mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - vault-data:/root/.log/vault
    env_file:
      - .env
    restart: unless-stopped

volumes:
  vault-data:
